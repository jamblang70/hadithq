[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --workspace=backend"]

[phases.build]
cmds = ["npm run build --workspace=backend"]

[start]
cmd = "node backend/dist/index.js"
